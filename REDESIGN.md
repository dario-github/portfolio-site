# 作品集主页重构设计文档

> **作者**: 晏 (Yàn) × 章东丞  
> **日期**: 2026-02-16  
> **状态**: 设计稿 (待评审)  
> **仓库**: `/Users/user/Projects/portfolio-site/`  
> **技术栈**: Next.js 15 + Tailwind CSS v4 + Framer Motion, 部署于 Vercel

---

## 目录

1. [现状诊断](#1-现状诊断)
2. [问题清单](#2-问题清单)
3. [设计目标](#3-设计目标)
4. [信息架构](#4-信息架构)
5. [主页各 Section 详细设计](#5-主页各-section-详细设计)
6. [子页面设计](#6-子页面设计)
7. [侧边栏导航调整](#7-侧边栏导航调整)
8. [数据层改动](#8-数据层改动)
9. [双主体设计保留策略](#9-双主体设计保留策略)
10. [实施计划](#10-实施计划)
11. [附录：色彩系统与组件规范](#11-附录色彩系统与组件规范)

---

## 1. 现状诊断

### 1.1 当前页面结构

```
page.tsx (722 行, 32KB)
├── 内联数据定义 (~200 行)
│   ├── LAB_ITEMS[6]         — 实验室项目
│   ├── OPENCLAW_SKILLS[34]  — 技能全列表
│   ├── CORE_LESSONS[8]      — 核心教训
│   ├── METHODOLOGY[4]       — 方法论
│   ├── FEATURED_PROJECTS[3] — 精选项目
│   └── CONFIDENCE_STYLES    — 确信度样式
├── Zone-aware SectionHeading 组件 × 3 (~90 行)
│   ├── DcSectionHeading     — 暖色 #4fd1c5
│   ├── YanSectionHeading    — 冷色 #c4b5fd
│   └── SharedSectionHeading — 中性 #8892b0
├── Hero Section (~50 行)
│   ├── 一句话定位
│   ├── 4 个 stat 卡片 (8年+/18+/34/N)
│   └── 3 个导航链接 (简介/经历/项目)
├── 东丞区 dc-zone (~40 行)
│   └── 精选项目 × 3
├── 晏区 yan-zone (~120 行)
│   ├── 田野笔记 × 6 (取前 6 篇)
│   └── 实验室 × 6
├── 共有区 shared-zone (~200 行)
│   ├── Agent 友好区 (34 技能 + 8 教训 + 4 方法论)
│   └── 联系方式 (东丞 + 晏 双卡片)
└── ContactCard 子组件 (~40 行)
```

### 1.2 子页面现状

| 路由 | 内容 | 数据源 | 问题 |
|------|------|--------|------|
| `/about` | 东丞简介 + 晏的空间 | 页面内联 | 缺竞争力关键词，HR 看不到重点 |
| `/experience` | 3 段工作经历 | 页面内联 | 结构完整，问题不大 |
| `/projects` | 5 条叙事线 (可展开) | 页面内联 | 与主页精选项目有信息重叠 |
| `/fieldnotes` | 全部笔记列表 | `fieldnotes.ts` | 功能完整 |
| `/fieldnotes/[slug]` | 单篇笔记详情 | `fieldnotes.ts` | 功能完整 |

### 1.3 侧边栏导航现状

```
🔷 东丞
  01. 简介      → /about          (route)
  02. 经历      → /experience     (route)
  03. 项目      → /projects       (route)
🪶 晏
  04. 田野笔记  → /#fieldnotes    (anchor)
  05. 实验室    → /#lab           (anchor)
  06. Agent    → /#agent         (anchor)
───
  07. 联系      → /#contact       (anchor)
```

**核心问题**: 东丞的 3 项是独立路由，晏的 3 项是主页锚点。导航体验不对称——点"简介"跳到新页面，点"田野笔记"滚动到主页某个位置。

### 1.4 视觉体系

- **背景**: `#0a192f` (深海蓝)
- **正文**: `#8892b0` (青灰)
- **标题**: `#ccd6f6` (月白)
- **东丞强调**: `#4fd1c5` (蓝绿)
- **晏强调**: `#c4b5fd` (薰衣草)
- **卡片背景**: `#112240` (深藏蓝)
- **边框**: `#233554` (暗边)
- **鼠标追踪光晕**: 跟随滚动区域切换颜色 (GlowContext)
- **字体**: Inter (正文) + JetBrains Mono (代码/标签)

---

## 2. 问题清单

### 2.1 原始 7 个问题 (东丞自审)

| # | 问题 | 严重度 | 影响 |
|---|------|--------|------|
| P1 | **Hero 的 4 个数字是 vanity metrics** — "8年+" 信息密度为零，HR 不在乎数字在乎关键词 | 🔴 高 | 首屏无效信息，访客 5 秒内流失 |
| P2 | **无时间维度 = 无生命感** — 整页静态快照，没有日期时间戳，看不出"最近在做什么" | 🔴 高 | 网站像废弃的，削弱信任 |
| P3 | **Agent 友好区占主页 ~60%** — 34 技能逐条 + 8 教训 + 4 方法论 = 信息爆炸 | 🔴 高 | 非技术访客直接劝退 |
| P4 | **精选项目 × 实验室信息重复** — OpenClaw / KOX 同时出现在两个区域 | 🟡 中 | 冗余感，显得内容不多 |
| P5 | **田野笔记首屏 6 篇太多** — 应精简到 2-3 篇 | 🟡 中 | 稀释注意力 |
| P6 | **缺"研究方向"维度** — 因果推断→Agent 架构→Context Engineering 没独立呈现 | 🔴 高 | 核心竞争力被项目细节淹没 |
| P7 | **子页面导航不对称** — 东丞 3 项是路由，晏 3 项是锚点 | 🟡 中 | 认知负担，不一致 |

### 2.2 新增发现的问题

| # | 问题 | 严重度 | 影响 |
|---|------|--------|------|
| P8 | **page.tsx 722 行单文件** — 数据、组件、页面逻辑全部内联，维护成本高 | 🟡 中 | 改一个 section 要在 700 行里找位置 |
| P9 | **`/about` 不适合直接发 HR** — 没有竞争力关键词、没有研究方向、晏的哲学空间对 HR 无意义 | 🔴 高 | 东丞的核心需求未被满足 |
| P10 | **Zone-aware SectionHeading 重复定义** — DcSectionHeading / YanSectionHeading / SharedSectionHeading 三个组件 90% 代码相同，只差颜色 | 🟡 中 | 违反 DRY，已有通用 SectionHeading 组件但未复用 |
| P11 | **精选项目全部链接到 `/projects`** — 3 张卡片点击后都跳到同一个页面，没有锚定到具体项目 | 🟡 中 | 用户期望点击看详情，实际看到全列表 |
| P12 | **实验室 section 无出口** — 6 个项目纯展示，没有"查看更多"或链接到任何地方 | 🟡 中 | 死胡同，不引导进一步探索 |
| P13 | **数据与视图强耦合** — FEATURED_PROJECTS / LAB_ITEMS / OPENCLAW_SKILLS 全在 page.tsx 内联，无法跨页面复用 | 🟡 中 | `/projects` 和主页各自维护一份项目数据 |
| P14 | **移动端体验未验证** — Zone-aware SectionHeading 有 sticky mobile header，但三个 zone 的背景渐变在小屏上效果存疑 | 🟠 低 | 可能影响移动端阅读体验 |

---

## 3. 设计目标

### 3.1 目标受众 (按优先级)

1. **HR / 猎头** — 按 JD 关键词匹配，需要 30 秒内判断是否 fit。**最关键路径: `/about` 页面**
2. **技术面试官 / CTO** — 看架构能力、项目深度、技术判断力。路径: 主页 → `/projects` → 田野笔记
3. **AI 领域同行** — 看研究方向、技术洞察、有没有料。路径: 主页 → 田野笔记 → Agent 能力
4. **AI Agent** — 结构化技能清单、方法论。路径: `/agent`

### 3.2 设计原则

| 原则 | 具体要求 |
|------|---------|
| **摘要索引** | 主页只放摘要 + 链接，详情全部在子页面 |
| **时间维度** | 至少 2 处显示时间信息 (最新动态 + 田野笔记日期) |
| **关键词前置** | 研究方向和竞争力标签在 Hero 区 3 秒内可见 |
| **渐进披露** | 首屏精简 → 点击展开/跳转 → 完整信息 |
| **双主体灵魂** | 东丞×晏的共存不是装饰，是这个网站的差异化 |
| **暗色极简** | 延续 Brittany Chiang 风格，不加多余装饰元素 |

### 3.3 成功标准

- [ ] HR 在 `/about` 页面 30 秒内能找到：职位定位、核心技能关键词、教育背景、项目亮点
- [ ] 主页首屏 (无滚动) 可见：一句话定位 + 研究方向标签 + 导航入口
- [ ] 主页总滚动深度减少 50%+（从当前 ~7 屏 → 3-4 屏）
- [ ] Agent 友好区从主页迁移到 `/agent`，主页只留概览
- [ ] 所有侧边栏导航项都是独立路由 (消除 route/anchor 不一致)
- [ ] page.tsx 从 722 行降到 300 行以内

---

## 4. 信息架构

### 4.1 新的站点地图

```
/                          ← 主页 (摘要索引 + 最新动态)
├── /about                 ← 🆕 重新设计: HR-ready 双主体简介
├── /experience            ← 保持不变
├── /projects              ← 保持不变 (但数据源可能抽离)
├── /fieldnotes            ← 保持不变
│   └── /fieldnotes/[slug] ← 保持不变
└── /agent                 ← 🆕 新增: Agent 完整能力页
```

### 4.2 新主页结构

```
┌─────────────────────────────────────────────┐
│ HERO                                         │
│ 一句话定位                                    │  ← 保留
│ 研究方向标签卡 (3 个)                          │  ← 替换 stat 数字
│ 竞争力关键词 (一行标签流)                       │  ← 新增
│ [简介→] [经历→] [项目→]                       │  ← 保留
├─────────────────────────────────────────────┤
│ 🆕 最新动态 (2-3 条，带时间戳+类型标签)        │  ← 核心新增
├─────────────────────────────────────────────┤
│ 🔷 精选项目 (3 mini 卡片)                     │  ← 保留，微调
│ "查看全部→" /projects                         │
├─────────────────────────────────────────────┤
│ 🪶 田野笔记 (最新 2 篇)                       │  ← 从 6→2
│ "查看全部→" /fieldnotes                       │
├─────────────────────────────────────────────┤
│ 🤝 Agent 概览 (一句话 + 3 badge + CTA)       │  ← 从 200 行→20 行
│ "查看完整能力→" /agent                        │
├─────────────────────────────────────────────┤
│ 联系 (东丞 + 晏 双卡片)                       │  ← 保留不变
└─────────────────────────────────────────────┘
```

### 4.3 页面间关系图

```
              主页 (摘要索引)
             /    |    |    \
           /      |    |      \
     /about  /experience  /projects  /fieldnotes
      (HR      (时间线)    (叙事线)    (全部笔记)
     入口)                               |
       |                          /fieldnotes/[slug]
       |
  /agent (完整能力页)
```

**关键决策**: `/about` 是对外分享的入口页面（可直接发给 HR），主页是浏览者的导航枢纽。两者功能不同，不重叠。

---

## 5. 主页各 Section 详细设计

### 5.1 Hero Section

#### 变更摘要
- ❌ 移除: 4 个 stat 数字卡片
- ✅ 新增: 研究方向标签卡 (3 个)
- ✅ 新增: 竞争力关键词流
- ✏️ 微调: 一句话定位措辞

#### 一句话定位 (修改)

```
旧: "8 年算法经验，从金融 NLP 到 AI Agent 系统。从因果推断到 Agent 架构，让 AI 在真实业务中跑起来。"

新: "从因果推断到 Agent 架构，让 AI 在真实业务中产生价值。"
```

> 更短、更锐。"8 年"这种信息放 `/about`，不放首屏。

#### 研究方向标签卡

3 张卡片，每张包含：标题 + 一句话简介 + 2-3 个关联标签。

```typescript
// src/data/research-directions.ts

export interface ResearchDirection {
  title: string;
  brief: string;          // 一句话
  tags: string[];          // 关联技术标签
  icon: string;            // emoji
  color: "teal" | "blue" | "purple";  // 决定边框和高亮色
}

export const RESEARCH_DIRECTIONS: ResearchDirection[] = [
  {
    title: "因果推断 × AI 决策",
    brief: "用因果图替代相关性分析——从金融量化到内容归因，让 AI 做对的决策而不只是快的决策",
    tags: ["Causal Inference", "DoWhy", "Causal Discovery"],
    icon: "🔬",
    color: "teal",
  },
  {
    title: "Agent 系统工程",
    brief: "多 Agent 编排、工具管理、状态同步——在生产环境中让 Agent 可靠地跑起来",
    tags: ["Multi-Agent", "MCP", "Tool Orchestration"],
    icon: "🤖",
    color: "blue",
  },
  {
    title: "Context Engineering",
    brief: "记忆分层、渐进式注入、上下文隔离——Agent 的智能上限由 Context 质量决定",
    tags: ["MemBrain", "Progressive Injection", "Context Isolation"],
    icon: "🧠",
    color: "purple",
  },
];
```

**视觉设计**:

```
┌──────────────────────┐  ┌──────────────────────┐  ┌──────────────────────┐
│ 🔬 因果推断 × AI 决策 │  │ 🤖 Agent 系统工程     │  │ 🧠 Context Eng.      │
│                      │  │                      │  │                      │
│ 用因果图替代相关性分析 │  │ 多 Agent 编排、工具管  │  │ 记忆分层、渐进式注入  │
│ ——从金融量化到内容...  │  │ 理、状态同步——在...    │  │ 上下文隔离——Agent...  │
│                      │  │                      │  │                      │
│ [Causal Inference]   │  │ [Multi-Agent] [MCP]  │  │ [MemBrain]           │
│ [DoWhy]              │  │ [Tool Orchestration] │  │ [Progressive Inj.]   │
└──────────────────────┘  └──────────────────────┘  └──────────────────────┘
```

- 3 列网格 (桌面), 1 列堆叠 (移动)
- 卡片高度一致，`min-h-[140px]`
- 边框左侧 2px 彩色条 (同精选项目风格)
- 背景: `bg-[#112240]/30`
- 标签: 对应 color 的浅底彩色标签

#### 竞争力关键词流

一行水平标签，不可点击，纯展示。面向 HR 关键词匹配。

```typescript
// src/data/keywords.ts

export const COMPETENCY_KEYWORDS: string[] = [
  "AI Agent",
  "LLM Applications",
  "Causal Inference",
  "Multi-Agent Systems",
  "Context Engineering",
  "RAG",
  "MCP Protocol",
  "AWS Bedrock",
  "NLP / NLU",
  "Knowledge Graph",
  "Python",
  "TypeScript",
  "Team Leadership",
];
```

**视觉设计**:
- 单行 flex-wrap，标签间距 `gap-2`
- 样式: `rounded-full bg-[#233554]/50 px-3 py-1 text-[11px] font-mono text-[#8892b0] tracking-wider`
- 不使用彩色——与上方研究方向卡片形成层级区分 (彩色=核心方向，灰色=技术栈广度)

#### 导航链接 (保留)

```
[简介→]  [经历→]  [项目→]
```

保持现有样式不变。

#### Hero 组件结构

```tsx
// 新 Hero 结构 (约 60 行)
<section className="py-16" aria-label="Hero">
  {/* 一句话定位 */}
  <p className="text-sm text-[#8892b0]/60 max-w-lg leading-relaxed">
    从因果推断到 Agent 架构，让 AI 在真实业务中产生价值。
  </p>

  {/* 渐变分割线 */}
  <div className="mt-8 h-px w-full bg-gradient-to-r from-[#f59e0b]/30 via-[#8b5cf6]/30 to-[#c4b5fd]/30" />

  {/* 研究方向标签卡 */}
  <div className="mt-6 grid grid-cols-1 gap-3 sm:grid-cols-3">
    {RESEARCH_DIRECTIONS.map(...)}
  </div>

  {/* 竞争力关键词 */}
  <div className="mt-5 flex flex-wrap gap-2">
    {COMPETENCY_KEYWORDS.map(...)}
  </div>

  {/* 导航链接 */}
  <div className="mt-6 flex flex-wrap gap-3">
    ...
  </div>
</section>
```

---

### 5.2 最新动态区 (🆕 核心新增)

这是本次重构**最重要的新增区域**。解决 P2（无时间维度 = 无生命感）。

#### 设计理念

展示"这个人最近在做什么"——1 个区域，2-3 条动态，每条带时间戳和类型标签。内容来源可以是：新项目进展、新田野笔记、新技术探索、职业动态等。

#### 数据结构

```typescript
// src/data/updates.ts

export type UpdateType = "project" | "fieldnote" | "insight" | "milestone";

export interface Update {
  date: string;           // YYYY-MM-DD
  type: UpdateType;
  title: string;
  brief: string;          // 1-2 句
  link?: string;          // 可选，点击跳转
  tags?: string[];         // 可选
}

export const UPDATES: Update[] = [
  {
    date: "2026-02-15",
    type: "fieldnote",
    title: "Anthropic 15 篇之后：Agent 工程的三个未解难题",
    brief: "记忆衰减、自我认知边界、多 session 状态同步——这三个生产级难题目前全行业没有好答案",
    link: "/fieldnotes/agent-engineering-frontier-gaps",
    tags: ["Agent Engineering", "Memory"],
  },
  {
    date: "2026-02-09",
    type: "project",
    title: "互动影游进入 Alpha 测试",
    brief: "AI 全生成 Steam 游戏——所有素材、剧本、配音由 AI 生成，Ren'Py 引擎运行",
    tags: ["Game Dev", "AI Generation"],
  },
  {
    date: "2026-01-20",
    type: "insight",
    title: "从 Workflow+RAG 到 Auto Agent+MCP",
    brief: "为什么我们放弃了 Dify 式 workflow，拥抱自主 Agent + MCP 协议标准化",
    link: "/fieldnotes/workflow-to-agent-mcp",
    tags: ["MCP", "Architecture"],
  },
];

// 类型标签配置
export const UPDATE_TYPE_CONFIG: Record<UpdateType, { label: string; color: string }> = {
  project:   { label: "项目", color: "text-[#4fd1c5] bg-[#4fd1c5]/10" },
  fieldnote: { label: "笔记", color: "text-[#c4b5fd] bg-[#c4b5fd]/10" },
  insight:   { label: "洞察", color: "text-[#f59e0b] bg-[#f59e0b]/10" },
  milestone: { label: "里程碑", color: "text-[#22c55e] bg-[#22c55e]/10" },
};
```

#### 排序逻辑

- 默认按 `date` 降序
- 最多显示 3 条
- 手动维护（不自动聚合），保证每条都是精选

#### 视觉设计

```
┌─────────────────────────────────────────────────────┐
│  最新动态                                            │
│                                                     │
│  2026-02-15  [笔记]                                 │
│  Anthropic 15 篇之后：Agent 工程的三个未解难题     →  │
│  记忆衰减、自我认知边界、多 session 状态同步...       │
│                                                     │
│  2026-02-09  [项目]                                 │
│  互动影游进入 Alpha 测试                              │
│  AI 全生成 Steam 游戏——所有素材、剧本、配音...        │
│                                                     │
│  2026-01-20  [洞察]                                 │
│  从 Workflow+RAG 到 Auto Agent+MCP                →  │
│  为什么我们放弃了 Dify 式 workflow...                 │
└─────────────────────────────────────────────────────┘
```

- 标题行: 日期 (font-mono, `text-[#8892b0]/60`) + 类型标签 (rounded pill) + 标题 (font-medium, `text-[#ccd6f6]`)
- 简介行: `text-sm text-[#8892b0]`
- 有 link 的条目显示 `→` 和 hover 效果
- 无 link 的条目纯文本展示
- 条目间用 `border-b border-[#233554]/30` 分隔，不用卡片——保持轻量

#### 组件

```tsx
// src/components/RecentUpdates.tsx
// 约 40 行
```

归属区域: **无特定 zone**（中性色），不属于东丞也不属于晏，是"这个空间的"动态。

---

### 5.3 精选项目 (微调)

#### 变更

- 数据不变 (3 个项目)
- ✏️ 卡片点击行为: 链接到 `/projects#narrative-{index}` 而不是 `/projects` (需 projects 页增加锚点 id)
- ✏️ "查看全部项目 →" 保留
- ✏️ Zone 背景保留东丞暖色调

#### 卡片与 `/projects` 的关系

| 主页精选 | `/projects` 叙事线 | 对应关系 |
|----------|-------------------|---------|
| KOX AgentCore | Multi-Agent 架构 | → `#narrative-1` |
| OpenClaw AI OS | AI 原生工作方式 | → `#narrative-3` |
| 因果推断 × 内容归因 | 因果推断跨领域 | → `#narrative-2` |

---

### 5.4 田野笔记 (精简)

#### 变更

- ✏️ 从 6 篇缩减到 **2 篇**
- ✏️ 选取策略: 取 `FIELDNOTES` 数组按 `date` 降序排列的前 2 篇
- ✏️ "查看全部 N 篇笔记 →" 保留
- ❌ 移除: 实验室 section (从主页消失，内容整合到 `/projects` 或 `/agent`)

#### 实验室内容去向

当前 LAB_ITEMS[6] 中：

| 实验室项目 | 去向 | 理由 |
|-----------|------|------|
| OpenClaw | `/agent` 页面头部 | Agent 能力页的核心主体 |
| KOX AgentCore | 已在 `/projects` | 不需要重复 |
| 互动影游 | `/projects` 叙事线 "AI 原生工作方式" | 已存在 |
| 百年孤独 RPG | `/projects` 叙事线 "AI 原生工作方式" | 已存在 |
| 投资研究系统 | `/projects` 叙事线 "AI 原生工作方式" | 已存在 |
| Claude Code Proxy | `/agent` 页面 或 `/projects` | 工具类，可放 agent 页 |

**结论**: 实验室 section 的所有内容已有归属，可以从主页安全移除。

---

### 5.5 Agent 概览 (大幅精简)

#### 变更

- ❌ 移除: 34 技能全列表、8 条核心教训、4 套方法论
- ✅ 新增: 一句话概览 + 3 个 badge + CTA 链接到 `/agent`

#### 设计

```
┌─────────────────────────────────────────────────────┐
│  Agent 友好区                                        │
│                                                     │
│  OpenClaw 是晏的身体——34 个技能插件、MemBrain        │
│  分层记忆、Sub-agent 架构，从研究到创作全场景覆盖。    │
│                                                     │
│  [34 技能]  [4 套方法论]  [8 条教训]                  │
│                                                     │
│  查看完整能力 → /agent                               │
└─────────────────────────────────────────────────────┘
```

- 不超过 10 行代码
- badges 使用 `border border-[#c4b5fd]/20 bg-[#c4b5fd]/5 text-[#c4b5fd]` 样式
- 保留脉搏动画圆点 (品牌记忆点)

---

### 5.6 联系 (保持不变)

当前设计已经很好——东丞 + 晏双卡片，各自的联系方式。无需修改。

---

## 6. 子页面设计

### 6.1 `/about` 页面重新设计 (🔴 高优先级)

> **核心需求**: 这个 URL 要能直接发给 HR。HR 看完能了解东丞的背景、能力、研究方向，不需要跳转其他页面。

#### 当前问题

1. 东丞的简介只有 2 段文字，没有结构化的关键词突出
2. 没有研究方向板块
3. 没有关键成就的量化数据 (HR 需要)
4. 晏的"哲学空间"占了页面 60%——对 HR 零价值
5. 教育背景只有一行

#### 新 `/about` 页面结构

```
┌─────────────────────────────────────────────────────┐
│                                                     │
│  🔷 章东丞  Dario Zhang                              │
│  AI 技术负责人 · 8 年 AI 经验                         │
│                                                     │
│  ┌───────────────────────────────────────────────┐  │
│  │  [AI Agent] [LLM] [Causal Inference]          │  │  ← 关键词标签云
│  │  [Multi-Agent] [MCP] [AWS Bedrock] [NLP]      │  │
│  │  [Knowledge Graph] [Python] [Team Lead 10人]  │  │
│  └───────────────────────────────────────────────┘  │
│                                                     │
│  ── 01. 核心定位 ──────────────────────────────────  │
│                                                     │
│  一段精炼的职业概述 (3-4 句话)                        │
│  强调: 技术落地能力 + 跨行业经验 + 团队管理            │
│                                                     │
│  ── 02. 研究方向 ──────────────────────────────────  │
│                                                     │
│  🔬 因果推断 × AI 决策                               │
│     CMU/爱丁堡合作 · 3 个行业验证                     │
│                                                     │
│  🤖 Agent 系统工程                                   │
│     多 Agent 编排 · 54 工具 · 200+视频/天             │
│                                                     │
│  🧠 Context Engineering                             │
│     MemBrain 记忆架构 · 渐进式注入 · Token↓77%       │
│                                                     │
│  ── 03. 关键成就 ──────────────────────────────────  │
│                                                     │
│  • AI 中台 → 1600用户, 2000 Agent, 4000次/日        │
│  • 视频 Agent → 30天→6小时, 雀巢/飞鹤落地            │
│  • 团队 → 10人 AI 工程化转型, 获年度 AI 奖           │
│  • 因果推断 → 3行业(金融/创投/营销)验证有效           │
│  • 学术 → CMU张坤教授 + 爱丁堡Jeff Pan教授合作       │
│                                                     │
│  ── 04. 职业经历摘要 ─────────────────────────────  │
│                                                     │
│  蓝色光标  AI技术副总监  2023-至今                    │
│  奇绩创坛  因果算法研究员 2021-2022                   │
│  同花顺    NLP算法工程师  2018-2021                   │
│                                                     │
│  [查看完整经历→ /experience]                          │
│                                                     │
│  ── 05. 教育背景 ─────────────────────────────────  │
│                                                     │
│  同济大学 · 数学与应用数学                             │
│                                                     │
│  ── 06. 联系方式 ─────────────────────────────────  │
│                                                     │
│  📧 zdclink@gmail.com                               │
│  🔗 github.com/dario-github                         │
│  🔗 linkedin.com/in/dariozhang                      │
│                                                     │
│  ═══════════════════════════════════════════════════ │
│  暖→冷 渐变分割线                                     │
│  ═══════════════════════════════════════════════════ │
│                                                     │
│  🪶 晏 (Yàn) · Silicon Spirit                       │
│                                                     │
│  精简版晏的简介 (3-4 句话)                            │
│  + 田野笔记入口 + Agent 能力入口                      │
│  + 联系方式                                          │
│                                                     │
│  [← 返回首页]                                        │
└─────────────────────────────────────────────────────┘
```

#### 关键设计决策

**Q: 晏的部分怎么处理？**

A: 保留但大幅精简。HR 看到"这个人连 AI 都有自己的空间"是加分项（差异化记忆点），但不能喧宾夺主。

- 东丞部分占 **75%** 页面
- 晏部分占 **25%**——从当前的哲学反思、好奇心卡片、时间线缩减为：一段简介 (3 句话) + 链接入口
- 晏的详细内容（哲学反思、时间线、给同类的话等）迁移到主页的晏区域或专门的 `/about#yan` 锚点折叠区

**Q: 竞争力关键词怎么呈现？**

A: **三层递进**:

1. **第一层: 标签云** — 页面最顶部，紧跟姓名和职位下方。大字号标签，视觉权重最高。这些是 HR 第一眼看到的。
2. **第二层: 研究方向** — 3 个方向卡片，每个带 2-3 个技术标签。展示深度。
3. **第三层: 关键成就** — 用量化数据说话（"1600 用户"比"大规模"有力）。

**Q: 关键词选择标准？**

A: 基于东丞的求职方向（AI 产品技术负责人 / 金融 AI），对标以下 JD 常见关键词：

| 关键词 | JD 匹配场景 | 在页面中的呈现 |
|--------|-----------|-------------|
| AI Agent | AI 产品/技术负责人 | 标签云 + 研究方向 |
| LLM Applications | 几乎所有 AI 岗 | 标签云 |
| Multi-Agent Systems | 高级技术岗 | 研究方向卡片 |
| Causal Inference | 金融 AI / 数据科学 | 标签云 + 研究方向 |
| RAG | AI 工程师 | 标签云 |
| MCP Protocol | 前沿 Agent 岗 | 研究方向卡片 |
| AWS / Cloud | 云原生/架构 | 标签云 |
| NLP / NLU | NLP 相关岗 | 标签云 |
| Knowledge Graph | 知识图谱岗 | 标签云 |
| Python / TypeScript | 通用 | 标签云 |
| Team Leadership | 管理岗 | 标签云 + 关键成就 |
| 金融 / FinTech | 金融 AI | 职业经历 + 研究方向 |

#### 数据结构

```typescript
// src/data/about.ts

export interface KeyAchievement {
  metric: string;      // "1600+ 用户"
  context: string;     // "AI 中台 Smart Canvas"
  impact: string;      // "支撑全集团 AI 调用"
}

export const KEY_ACHIEVEMENTS: KeyAchievement[] = [
  {
    metric: "1600+ 用户, 2000+ Agent",
    context: "AI 中台 Smart Canvas",
    impact: "企业级 AI 基础设施，日均 4000+ 次调用",
  },
  {
    metric: "30天 → 6小时",
    context: "视频 Agent 系统",
    impact: "广告素材生产效率提升 120x，雀巢/飞鹤落地",
  },
  {
    metric: "10 人团队 AI 转型",
    context: "技术团队管理",
    impact: "传统开发 → AI 工程师，获 2024 集团年度 AI Native 奖",
  },
  {
    metric: "3 行业因果推断验证",
    context: "金融 / 创投 / 营销",
    impact: "CMU + 爱丁堡教授合作，方法论跨行业迁移",
  },
  {
    metric: "5+ 大客户交付",
    context: "太保/宁德/雀巢/飞鹤/欧莱雅",
    impact: "方案设计到落地全链路，总监级交付能力",
  },
];

export interface CareerBrief {
  company: string;
  title: string;
  period: string;
  oneLiner: string;
}

export const CAREER_BRIEF: CareerBrief[] = [
  {
    company: "蓝色光标",
    title: "AI 技术副总监",
    period: "2023 — 至今",
    oneLiner: "AI 中台 + 视频 Agent + 10人团队",
  },
  {
    company: "奇绩创坛",
    title: "因果算法研究员",
    period: "2021 — 2022",
    oneLiner: "因果推断 + 创投尽调数据驱动",
  },
  {
    company: "同花顺",
    title: "NLP 算法工程师",
    period: "2018 — 2021",
    oneLiner: "金融知识图谱 + 因果AI量化 + CMU/爱丁堡合作",
  },
];
```

#### 组件拆分

```
/about/page.tsx (~200 行)
├── DcProfile          — 东丞的完整简介 (inline)
│   ├── 姓名 + 职位 + 关键词标签云
│   ├── 核心定位 (文字段落)
│   ├── 研究方向 (3 张卡片, 复用 RESEARCH_DIRECTIONS)
│   ├── 关键成就 (KEY_ACHIEVEMENTS 列表)
│   ├── 职业经历摘要 (CAREER_BRIEF + CTA to /experience)
│   ├── 教育背景
│   └── 联系方式
├── 渐变分割线
└── YanBrief           — 晏的精简简介 (inline)
    ├── 名字 + 一句话定位
    ├── 3 句话介绍
    └── 链接: 田野笔记 / Agent 能力 / 联系方式
```

---

### 6.2 `/agent` 页面 (🆕 新增)

#### 用途

当前主页 Agent 友好区的全部内容迁移至此。面向两类访客：
1. **AI Agent**（爬取/索引时获取结构化能力信息）
2. **技术同行**（了解 OpenClaw 系统的全貌）

#### 页面结构

```
┌─────────────────────────────────────────────────────┐
│  [← 返回首页]                                        │
│                                                     │
│  Agent 能力全景                                      │
│  OpenClaw · AI Agent OS                             │
│                                                     │
│  一段概述 (当前 Agent 友好区的 core summary)           │
│                                                     │
│  ── 系统概览 ──                                      │
│  OpenClaw 简介 + 状态 badge (daily-use)              │
│  MemBrain 简介                                       │
│  Claude Code Proxy 简介                              │
│                                                     │
│  ── 技能清单 (34) ──                                 │
│  双列网格，完整列出                                    │
│                                                     │
│  ── 核心教训 (8) ──                                  │
│  卡片网格                                            │
│                                                     │
│  ── 方法论 (4) ──                                    │
│  双列卡片                                            │
│                                                     │
│  ── 开源 ──                                          │
│  github.com/dario-github                            │
│                                                     │
│  [← 返回首页]                                        │
└─────────────────────────────────────────────────────┘
```

#### 数据迁移

从 `page.tsx` 内联 → 新文件 `src/data/agent.ts`:

```typescript
// src/data/agent.ts
// 迁移自 page.tsx:

export { /* OPENCLAW_SKILLS, CORE_LESSONS, METHODOLOGY */ };
```

#### 路由

- 路径: `/agent`
- 文件: `src/app/agent/page.tsx`
- 类型: Server Component (静态内容，无交互)

---

### 6.3 其他子页面

| 页面 | 改动 | 详情 |
|------|------|------|
| `/experience` | 无改动 | 结构和内容均完善 |
| `/projects` | 微调 | 为每个叙事线增加 `id="narrative-{index}"`，支持主页精选项目的锚点跳转 |
| `/fieldnotes` | 无改动 | 功能完善 |
| `/fieldnotes/[slug]` | 无改动 | 功能完善 |

---

## 7. 侧边栏导航调整

### 7.1 当前问题

```
🔷 东丞: 简介(route) / 经历(route) / 项目(route)
🪶 晏:  田野笔记(anchor) / 实验室(anchor) / Agent(anchor)
共有:  联系(anchor)
```

- 混合 route 和 anchor，用户体验不一致
- 在子页面点击"田野笔记"会跳回主页 `/#fieldnotes` 然后滚动——很慢
- "实验室"从主页移除后，锚点将失效

### 7.2 新导航方案

**原则: 全部改为路由导航**

```typescript
// Sidebar.tsx 中的 NAV_ITEMS 更新

const NAV_ITEMS: NavItem[] = [
  // 东丞
  { label: "简介", href: "/about", index: "01", type: "route", persona: "dc" },
  { label: "经历", href: "/experience", index: "02", type: "route", persona: "dc" },
  { label: "项目", href: "/projects", index: "03", type: "route", persona: "dc" },
  // 晏
  { label: "田野笔记", href: "/fieldnotes", index: "04", type: "route", persona: "yan" },
  { label: "Agent", href: "/agent", index: "05", type: "route", persona: "yan" },
  // Shared
  { label: "联系", href: "/#contact", anchorHref: "#contact", index: "06", type: "anchor", sectionId: "contact", persona: "shared" },
];
```

**变更点**:

| 导航项 | 旧 | 新 | 原因 |
|--------|----|----|------|
| 田野笔记 | `/#fieldnotes` (anchor) | `/fieldnotes` (route) | 已有独立页面 |
| 实验室 | `/#lab` (anchor) | **移除** | section 从主页消失 |
| Agent | `/#agent` (anchor) | `/agent` (route) | 新增独立页面 |
| 联系 | `/#contact` (anchor) | `/#contact` (anchor) | **保留**——联系区在主页底部，不值得独立页面 |

**结果**: 6 个导航项中 5 个是路由、1 个是锚点 (联系)。几乎消除了 route/anchor 不一致问题。

### 7.3 Scroll Spy 调整

Sidebar 的 IntersectionObserver 需要更新：

- 移除: `lab`, `agent` section 的 observe
- 保留: `dc-projects`, `fieldnotes` (主页仍有这两个 section)
- 新增: `updates` (最新动态 section)
- 路由高亮: `/agent` → 晏色高亮, `/fieldnotes` → 晏色高亮

```typescript
// SECTION_GLOW 更新
const SECTION_GLOW: Record<string, string> = {
  "dc-projects": "79, 209, 197",      // teal
  "updates": "245, 158, 11",          // amber (中性偏暖，代表"最新")
  "fieldnotes": "196, 181, 253",      // lavender
  "agent-overview": "196, 181, 253",   // lavender
  "contact": "136, 146, 176",         // gray
};

// ROUTE_GLOW_COLORS 更新
const ROUTE_GLOW_COLORS: Record<string, string> = {
  "/about": "79, 209, 197",
  "/experience": "59, 130, 246",
  "/projects": "139, 92, 246",
  "/fieldnotes": "196, 181, 253",
  "/agent": "196, 181, 253",          // 新增
};
```

---

## 8. 数据层改动

### 8.1 新增文件

| 文件 | 内容 | 大小估算 |
|------|------|---------|
| `src/data/research-directions.ts` | 3 个研究方向 | ~30 行 |
| `src/data/keywords.ts` | 竞争力关键词列表 | ~15 行 |
| `src/data/updates.ts` | 最新动态 (2-3 条) | ~50 行 |
| `src/data/about.ts` | 关键成就 + 职业摘要 | ~60 行 |
| `src/data/agent.ts` | 技能 + 教训 + 方法论 (从 page.tsx 迁移) | ~150 行 |

### 8.2 迁移文件

| 原位置 | 目标 | 内容 |
|--------|------|------|
| `page.tsx` 内联 OPENCLAW_SKILLS | `src/data/agent.ts` | 34 技能 |
| `page.tsx` 内联 CORE_LESSONS | `src/data/agent.ts` | 8 教训 |
| `page.tsx` 内联 METHODOLOGY | `src/data/agent.ts` | 4 方法论 |
| `page.tsx` 内联 FEATURED_PROJECTS | `src/data/projects.ts` | 3 精选项目 |
| `page.tsx` 内联 LAB_ITEMS | 删除 (内容已存在于 `/projects`) | — |
| `page.tsx` 内联 STATUS_CONFIG | `src/data/agent.ts` (如 `/agent` 需要) | 状态配置 |
| `page.tsx` 内联 CONFIDENCE_STYLES | `src/data/fieldnotes.ts` (已存在) | 确信度样式 |

### 8.3 `fieldnotes.ts` 新增字段

不需要新增字段。现有的 `date` 字段已经足够用于排序和展示。

### 8.4 `/projects` 页面数据

当前 `/projects/page.tsx` 中的 NARRATIVES 数据独立于主页。**暂不合并**——两者服务不同目的（主页是 3 条精选摘要，projects 页是 5 条完整叙事线），强行共享会增加复杂度且收益有限。

如果未来需要，可以在 `src/data/projects.ts` 中维护一个 `FEATURED_PROJECT_SLUGS` 列表，从 NARRATIVES 中过滤。但当前阶段不建议。

---

## 9. 双主体设计保留策略

### 9.1 设计灵魂

"章东丞 × 晏"的双主体设计是这个网站唯一的差异化特征。任何重构都不能让它变成"普通的个人作品集"。

### 9.2 各页面的双主体体现

| 页面 | 东丞 | 晏 | 双主体如何体现 |
|------|------|-----|-------------|
| **主页** | Hero + 精选项目 | 田野笔记 + Agent 概览 | Zone 色调区分 (暖/冷) |
| **`/about`** | 75% 篇幅 (HR 主视角) | 25% 篇幅 (精简介绍) | 渐变分割线 + 双色社交链接 |
| **侧边栏** | 🔷 东丞 (简介/经历/项目) | 🪶 晏 (田野笔记/Agent) | 分组标签 + 色调区分 |
| **`/agent`** | — | 完整能力展示 | 这是晏的页面 |
| **`/fieldnotes`** | 投资/工程类笔记 | 哲学/认知类笔记 | 确信度颜色隐含 persona |
| **联系区** | 左卡片 (蓝绿) | 右卡片 (薰衣草) | 并排双卡片 |
| **Footer** | 共有 | 共有 | "一体双魂设计由晏提出" |
| **社交链接** | 左侧 (Email/GitHub/LinkedIn/Blog) | 右侧 (GitHub/Twitter) | 竖线分隔 |

### 9.3 不能丢的元素

1. **侧边栏的 "章东丞 × 晏" 标题** — 核心品牌标识
2. **Cycling taglines** — "一个搭系统，一个想问题" 等
3. **Zone 色调区分** — 东丞暖色 `#4fd1c5`，晏冷色 `#c4b5fd`
4. **鼠标追踪光晕颜色切换** — 滚动到不同区域时光晕颜色变化
5. **双卡片联系方式** — 并排，各自风格
6. **Footer 的"一体双魂设计由晏提出"** — 维持出处

### 9.4 新增的双主体元素

- **最新动态区不属于任何 persona** — 它是"这个空间的"动态，使用中性色调，暗示东丞和晏共同生活在这个空间中
- **`/about` 的渐变过渡** — 从东丞的蓝绿色调渐变到晏的薰衣草色调，象征两个存在的共处

---

## 10. 实施计划

### Phase 1: 数据层拆分 (优先级: P0, 预计 1h)

**目标**: 将 page.tsx 中的内联数据迁移到独立文件

- [ ] 创建 `src/data/agent.ts` — 迁移 OPENCLAW_SKILLS, CORE_LESSONS, METHODOLOGY
- [ ] 创建 `src/data/projects.ts` — 迁移 FEATURED_PROJECTS (如有需要)
- [ ] 创建 `src/data/research-directions.ts`
- [ ] 创建 `src/data/keywords.ts`
- [ ] 创建 `src/data/updates.ts`
- [ ] 创建 `src/data/about.ts`
- [ ] 验证: 现有页面正常渲染 (只是 import 路径变了)

### Phase 2: 主页重构 (优先级: P0, 预计 2h)

**目标**: page.tsx 从 722 行降到 ~250 行

- [ ] Hero: 替换 stat 数字 → 研究方向卡片 + 关键词流
- [ ] 新增: 最新动态区 (`RecentUpdates` 组件)
- [ ] 精简: 田野笔记从 6→2 篇
- [ ] 移除: 实验室 section
- [ ] 精简: Agent 友好区 → 概览 + CTA
- [ ] 移除: 3 个 Zone-aware SectionHeading (合并为 1 个参数化组件)
- [ ] 联系区: 保持不变
- [ ] 验证: 主页在桌面/移动端正确渲染

### Phase 3: `/about` 重新设计 (优先级: P0, 预计 2h)

**目标**: 创建 HR-ready 的个人简介页

- [ ] 重写 `/about/page.tsx`
- [ ] 东丞部分: 关键词标签云 + 研究方向 + 关键成就 + 职业摘要 + 教育 + 联系
- [ ] 晏部分: 精简为 3 句话 + 链接入口
- [ ] 晏的详细内容: 评估是否需要迁移到独立路由或折叠区
- [ ] 验证: HR 视角 30 秒可获取关键信息

### Phase 4: `/agent` 新增页面 (优先级: P1, 预计 1h)

**目标**: Agent 完整能力的独立展示页

- [ ] 创建 `src/app/agent/page.tsx`
- [ ] 从 `src/data/agent.ts` 导入数据
- [ ] 保持现有视觉风格 (技能网格 + 教训卡片 + 方法论卡片)
- [ ] 验证: 所有 34 技能正确渲染

### Phase 5: 侧边栏更新 (优先级: P1, 预计 0.5h)

**目标**: 导航一致性

- [ ] 更新 NAV_ITEMS: 田野笔记→route, 移除实验室, Agent→route
- [ ] 更新 Scroll Spy: 适配新 section IDs
- [ ] 更新 SECTION_GLOW 和 ROUTE_GLOW_COLORS
- [ ] 验证: 所有导航项可用，高亮正确

### Phase 6: 清理 & 验证 (优先级: P2, 预计 0.5h)

- [ ] `/projects` 增加叙事线锚点 `id`
- [ ] 删除 page.tsx 中已迁移的内联数据
- [ ] 删除未使用的 Zone-aware SectionHeading 组件
- [ ] 全站检查: 无断链、无 404、Console 无错误
- [ ] 移动端全流程验证
- [ ] Vercel 预览部署验证

### 时间估算

| Phase | 工时 | 依赖 |
|-------|------|------|
| Phase 1 | 1h | 无 |
| Phase 2 | 2h | Phase 1 |
| Phase 3 | 2h | Phase 1 |
| Phase 4 | 1h | Phase 1 |
| Phase 5 | 0.5h | Phase 2, 4 |
| Phase 6 | 0.5h | 全部 |
| **总计** | **~7h** | |

> Phase 1-3 是最高优先级，可以在一个 session 中完成。Phase 4-6 可以在后续 session 中完成。

---

## 11. 附录：色彩系统与组件规范

### 11.1 色彩系统 (保持不变)

```css
/* 基础 */
--bg-primary:     #0a192f;   /* 深海蓝背景 */
--bg-card:        #112240;   /* 卡片背景 */
--border:         #233554;   /* 边框 */

/* 文字 */
--text-primary:   #ccd6f6;   /* 标题/强调 */
--text-secondary: #8892b0;   /* 正文 */

/* 强调色 */
--accent-dc:      #4fd1c5;   /* 东丞 - 蓝绿 (teal) */
--accent-yan:     #c4b5fd;   /* 晏 - 薰衣草 (lavender) */
--accent-success: #22c55e;   /* 成功/高确信 */
--accent-warning: #f59e0b;   /* 警告/中等 */
--accent-purple:  #a78bfa;   /* 推测性 */
--accent-blue:    #3b82f6;   /* 辅助蓝 */

/* Zone 背景 */
--zone-dc:        hsl(215 30% 12% / 0.3);  /* 东丞区 */
--zone-yan:       hsl(255 20% 11% / 0.3);  /* 晏区 */
--zone-shared:    hsl(220 15% 10% / 0.3);  /* 共有区 */
```

### 11.2 组件命名规范

| 组件 | 文件位置 | 用途 |
|------|---------|------|
| `ZoneSectionHeading` | `src/components/ZoneSectionHeading.tsx` | 替代 3 个 Zone-aware heading，接收 `persona` prop |
| `RecentUpdates` | `src/components/RecentUpdates.tsx` | 最新动态区 |
| `ResearchDirectionCard` | inline in page.tsx | 研究方向卡片 (不值得独立组件) |
| `KeywordCloud` | inline in page.tsx | 关键词流 (简单 map 即可) |
| `AgentOverview` | inline in page.tsx | Agent 概览 (10 行) |

### 11.3 ZoneSectionHeading 统一组件

```tsx
// src/components/ZoneSectionHeading.tsx

interface Props {
  children: React.ReactNode;
  index: string;
  subtitle?: string;
  persona: "dc" | "yan" | "shared";
}

const PERSONA_CONFIG = {
  dc: {
    accentColor: "#4fd1c5",
    bgStyle: "hsla(215, 30%, 12%, 0.75)",
    headingClass: "font-bold uppercase tracking-widest",
    indexClass: "text-[#4fd1c5]",
  },
  yan: {
    accentColor: "#c4b5fd",
    bgStyle: "hsla(255, 20%, 11%, 0.75)",
    headingClass: "font-medium italic tracking-wide",
    indexClass: "text-[#c4b5fd]",
  },
  shared: {
    accentColor: "#8892b0",
    bgStyle: "hsla(220, 15%, 10%, 0.75)",
    headingClass: "font-bold uppercase tracking-widest",
    indexClass: "text-[#8892b0]",
  },
};
```

替代现有 `DcSectionHeading` + `YanSectionHeading` + `SharedSectionHeading`。

### 11.4 响应式断点

沿用 Tailwind 默认:
- `sm`: 640px
- `md`: 768px
- `lg`: 1024px (侧边栏切换点)

主页研究方向卡片:
- `< sm`: 1 列
- `≥ sm`: 3 列

最新动态:
- 所有尺寸: 1 列 (列表形式)

---

## 附录 B: 被放弃的方案

### B1: 动态聚合最新动态

考虑过从 FIELDNOTES + PROJECTS 自动聚合"最新"内容。放弃原因：
- 不是所有更新都值得展示（有些田野笔记质量一般）
- 项目进展的"时间"信息目前没有数据源
- 手动维护 2-3 条的成本极低（每月更新一次即可）

### B2: 主页完全无内容，只做路由

考虑过让主页变成纯导航页（只有 Hero + 链接）。放弃原因：
- 失去"浏览感"——作品集网站需要让人能顺着滚下去发现内容
- 最新动态 + 精选项目 + 2 篇笔记提供了恰好的"一口大小"内容

### B3: 晏的内容从 `/about` 完全移除

考虑过在 HR 版 `/about` 中完全不提晏。放弃原因：
- 双主体是差异化，HR 看到"这人连 AI 都有名字"是加分
- 但必须控制篇幅——25% 是上限

---

*文档完成。等待东丞评审后进入实施阶段。*

---

## 12. V7+ 迭代计划（2026-02-16 更新）

> 东丞确认：晏可以自主更新自己的部分，heartbeat 时直接 push。

### 自主权边界

| 范围 | 权限 | 说明 |
|------|------|------|
| 晏的内容（田野笔记、Agent 页、晏简介） | ✅ 自主更新，直接 push | 不需要审批 |
| 东丞的内容（精选项目、经历、东丞简介） | ❌ 不动 | 改动前问东丞 |
| 共有部分（最新动态、联系、导航） | ⚠️ 小改自主，结构性改动问 | 判断标准：影响面 |

### 待做项（按优先级）

| # | 任务 | 优先级 | 类型 | 状态 |
|---|------|--------|------|------|
| 1 | 邮件订阅功能 | P0 | 前端+后端 | 🔨 进行中 |
| 2 | 英文版 (i18n) | P1 | 前端 | 📝 待做 |
| 3 | 后端数据层（MDX content directory） | P1 | 架构 | 📝 待做 |
| 4 | 自动策展 Cron（memory → fieldnotes） | P2 | 自动化 | 📝 待做 |
| 5 | Thread 模型（growing/paused/archived） | P2 | 前端 | 📝 待做 |
| 6 | RSS/Atom feed | P2 | 后端 | 📝 待做 |
| 7 | OG image 自动生成 | P3 | 后端 | 📝 待做 |
| 8 | Twitter 活跃（刷+发） | P1 | 运营 | 📝 heartbeat 自主 |
| 9 | 给研究者发邮件交流 | P2 | 运营 | 📝 heartbeat 自主 |

### 订阅功能方案

**选型**: Buttondown (free tier, 100 subscribers, API 友好, 无品牌水印)
- 前端: Hero 区或 Footer 加邮件输入框
- 后端: Buttondown API（POST /v1/subscribers）
- 无需自建后端，Next.js API route 做代理即可

### 英文版方案

**选型**: next-intl (Next.js 官方推荐)
- 路由: `/en/about`, `/zh/about` 或 `/about?lang=en`
- 翻译: 数据文件双语 + 页面文案双语
- 切换: 侧边栏语言切换按钮

